#include<stdio.h>

int main(){
    int a;
    scanf("%d", &a);
    int b;
    scanf("%d", &b);
    // printf("the variables are %d and second no. is %d after interchanging\n", a, b);
    // printf("the interchanged variables are %d and second no. is %d",b ,a);
    printf(" %d %d\n", a, b);
    printf(" %d %d",b, a);
    
    return 0;
} 