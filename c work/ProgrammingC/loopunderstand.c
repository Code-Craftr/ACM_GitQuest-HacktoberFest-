#include<stdio.h>

int main(){
    int count;
    scanf("%d",&count);
    // int i;
    // i = count;
    for(int i=0; i<=count; i++){
        printf("hello world %d\n", i);
    }
    return 0;
}