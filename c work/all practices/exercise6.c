#include<stdio.h>

int main(){
    float f = 144.0;
    printf("To convert %f fahrenheit into celcius the formula is 5/9x(F-32) and by using this formula the answer to this is %f", f, 5.0/9.0*(f-32));
    return 0;
}