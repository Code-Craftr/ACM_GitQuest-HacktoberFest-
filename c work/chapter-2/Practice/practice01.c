#include<stdio.h>

int main(){
    //which of the following is incorrect
    int a = 1;
    int b = 2;
    int c = 3*3;
    char dt='t'; //the char can only consist of only 1 character.
    printf("interger a %d interger b %d\n interger c %d and char dt %c", a, b, c, dt);
    return 0;
}