#include<stdio.h>

int main(){
    int x = 2;
    int y = 3;
    int z = 3;
    int k = 1;
    printf("The quesition is what is the answer of 3*x/y-z+k where k is %d Y is %d Z is %d and X is %d\n The answer to this quesition is %d", k, y, z, x, 3*x/y-z+k);
    return 0;
}