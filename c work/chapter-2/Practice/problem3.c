#include<stdio.h>

int main(){
    int a = 2452453;
    printf("to know if A %d is divisible by 97 or not we have to run a command like a%97 so if we get zero as answer we know that it is divisible by 97 lets check, the answer we are getting is %d this means it is not divisible by 2", a, a%97);

    return 0;
}